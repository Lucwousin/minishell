#subshell:
)
||
&&
|
(
()
(||)
(&&)
(|)
#redirections:
>
<
<<
>>
> ||
> (
>> &&
<< |
#pipelines:
echo |
echo | |
echo | ||
echo | &&
echo | )
#logic:
echo &&
echo ||
echo && )
echo || )
echo && |
echo || |
echo && &&
echo || &&
